signature : {
    @source = Attribute
    @target = Attribute
}

f = new @source->generator.name(@source->generator.init);
rparams = union(@source->generator->requires.name);
@source.name = map(f, rparams);
