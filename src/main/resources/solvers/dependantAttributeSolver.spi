signature : {
    @source = Attribute;
    length(@source->generator->requires.name) != 0;
}

let f = init(@source->generator.name,@source->generator->initparam.value);
let rparams = zip(@source->generator->requires.name);
@source.name = map(f, rparams);
