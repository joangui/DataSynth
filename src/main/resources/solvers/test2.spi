signature : {
    @source = attribute
    @target = attribute
}

bindings : {
    x = @source.id
    f = @target.func

    x1 = @source.degree
    x2 = @target.degree
    x3 = @source.edge
}

program : {
    fp = new f
    x = map(x, fp)
    y = union(x1,x2,x3)
}