signature : {
    @source = attribute
    @target = attribute
}

bindings : {
    x = @source.name
    f = @source.generator
    input = @source.input
}

program : {
    fp = new f(a)
    x = map(f, input)
}